name: docling-serve-cu128

on:
  push:
    branches:
      - main
    tags:
      - v*

  pull_request:

env:
  IMAGE_NAME: image-docling-serve-cu128

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run tests
        run: |
          if [ -f docker-compose.test.yml ]; then
            docker-compose --file docker-compose.test.yml build
            docker-compose --file docker-compose.test.yml run sut
          else
            docker build . --file docling-serve-cu128.Dockerfile
          fi

  push:
    needs: test

    runs-on: ubuntu-latest
    if: github.event_name == 'push'

    steps:
      - uses: actions/checkout@v2

      - name: Build image
        run: docker build . --file docling-serve-cu128.Dockerfile --tag $IMAGE_NAME

      - name: Log into Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u luohua13 --password-stdin

      - name: Push image to Docker Hub
        run: |
          IMAGE_ID=luohua13/docling-serve-cu128
          VERSION=v1.30.0

          IMAGE_ID=$(echo $IMAGE_ID | tr '[A-Z]' '[a-z]')

          echo IMAGE_ID=$IMAGE_ID
          echo VERSION=$VERSION

          docker tag $IMAGE_NAME $IMAGE_ID:$VERSION
          docker push $IMAGE_ID:$VERSION
